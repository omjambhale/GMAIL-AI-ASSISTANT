# Gmail AI Extension (V1)

**Goal**: Inject “Summarize” + “AI Reply Suggestions” into Gmail via Chrome extension.

## Structure
- extension/   ← Chrome extension code
- backend/     ← FastAPI server + OAuth + LLM calls
